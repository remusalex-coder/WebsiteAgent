# BusinessForge — Implementation Gap

Strictly prioritized. P0 = blocks the factory end-to-end. P1 = needed for a real first product. P2 = quality/scaling. P3 = future.

---

## P0 — blocking

### P0-1. Wire the stage ledger into `stage.ts` — DONE (commit `c86330a`)
- **Correction:** `lib/workflow/ledger.ts` turned out, on inspection, to be a different, already-correctly-scoped module (the retry/rebuild/reconcept counters Hermes's ceiling checks read, per `ARCHITECTURE_FREEZE.md` §F-04) — not the input/output content-addressing this row originally meant. The actual persistence gap was in `lib/workflow/hashes.ts` (`AddressedStage`/`shouldSkip`, tested alone, never persisted). Fixed there instead: added `StageLedger`/`recordStage`/`loadStageLedger` (atomic write, in-process lock, same pattern as `candidates.ts`'s index file) to `hashes.ts`, and wired `recordStage` into `runStage`'s single dispatch point — every real stage completion or failure gets one merged ledger entry, without touching any of the 22 case bodies.
- **Acceptance:** met — see `test/workflow/hashes.test.ts` and `test/workflow/stageLedgerWiring.test.ts`.

### P0-2. Skip-unchanged-stage resume — DONE (commit `b6da711` + follow-up), `resume.ts` REMOVED as a duplicate
- **Correction:** `lib/workflow/resume.ts`'s `planResume` was analyzed and **not** wired in — it would have been a second, parallel resume system alongside the one below, which the standing rule explicitly forbids. Removed (`lib/workflow/resume.ts`, `test/workflow/resume.test.ts`); confirmed zero other importers before deletion.
- **What was actually built:** `runStage` computes this call's content-addressed input (`opts` + the pre-stage job snapshot) and checks it against the ledger entry P0-1 now stamps with `outputPath: 'job.json'`; a match skips the entire 22-case switch and returns `job.json` as-is. `hermes` is excluded (real repair work + owns the loop/nextStage signal). A fresh `runJobFullWith` invocation still always starts at `create` — there is no separate "which stage to resume at" planner; each stage decides for itself, so a killed-and-restarted run cheaply replays the whole sequence and every already-done stage no-ops.
- **One real gap found and fixed in the same pass:** `intake`'s result carries `providers.pool`, which `runJobFullWith` reads to drive the `research` fan-out — a naive skip left this `undefined`, silently dropping the entire research phase on resume. Fixed by recomputing `pool` (cheap, deterministic config resolution, no model call) on an `intake` skip specifically.
- **Acceptance:** met — see `test/workflow/stageLedgerWiring.test.ts` (fresh job, identical-call skip, changed-call rerun, intake-pool-on-skip, hermes-never-skipped, killed-and-restarted `runJobFullWith`, failed-stage recording).

### P0-3 / T04. Real deployment — DONE (commit pending, this pass)
- **Correction:** `agents/lovableAgent.ts` does **not** throw `NotImplementedError` — on inspection it already delegates entirely to `lib/deploy/netlify.ts`'s real Netlify "Drop" integration (zips `site/`, creates/reuses a site, uploads, polls, returns a live URL or a recoverable `skipped`/`failed` status). That was true before this pass and is unchanged; the actual gap was elsewhere. `main.ts`'s classic pipeline already reached it via `lovableAgent`. The **production** pipeline (`scripts/n8n/stage.ts`'s `runStage`/`runJobFullWith` — the "factory" loop T01–T03 all target) had no `deploy` stage at all: it had `preflight` and `report` but nothing in between that actually published anywhere, so a "delivered" job's `finalOutput` was only ever the local `site/` path `hermes`'s deliver branch set. "Browser opens on a live URL" was unreachable from that pipeline regardless of how a run went.
- **What was built:** a new `deploy` `StageName`, sequenced between `preflight` and `report` (never publish something the gate just blocked; the report should reflect the real outcome). Its case body calls the *same* `lib/deploy/netlify.ts` `deployToNetlify` `main.ts` already uses — no second deploy implementation. Skipped (no network call) when `job.decision !== 'deliver'`. A missing `NETLIFY_DEPLOY_TOKEN` is `status: 'skipped'`, never `failed` (the token's own established contract), and leaves `finalOutput` as the local site path. A real `status: 'failed'` downgrades the job to `decision: 'escalate'` and preserves the last-known-good local `finalOutput` — a deploy failure can never surface as a false DELIVERED status. A successful deploy overwrites `finalOutput` with the live URL. The result is always recorded to `qa/deployment.json` and surfaced in the `report` stage's summary. Being an ordinary stage at the single `runStage` dispatch point, it inherits T01's ledger recording and T02's skip-on-resume for free — a resumed job with an unchanged decision does not re-deploy.
- **Acceptance:** met for the reachable boundary — see `test/workflow/deploy.test.ts` (skip-on-non-deliver with zero network calls; skip-status with an explicitly forced-empty token, real code path, `finalOutput` preserved; resume-skip via T02 on a second identical call; the `report` stage surfacing the recorded status; a pre-T04 run with no deployment file reporting `null`, not a guess). The `status: 'live'`/`status: 'failed'` branches require a real Netlify account and network access; they are covered by `deployToNetlify`'s own never-throws contract (it catches its own network/API errors, tested implicitly by every call in this suite exercising the try/catch structure with the skip branch) rather than a live-network unit test, per the standing "no real credentials in tests" rule. A documented manual run with a real `NETLIFY_DEPLOY_TOKEN` is the way to verify the live path end to end; not run in this pass since no such credential is available to this agent (see the Final Report's remaining-work note).

### P0-4. `main.ts` classic pipeline feeds `JobState` — DONE (commit `7a79ef2`)
- **What was built:** `main.ts`'s generic `step()` helper (its own resume/persist mechanism, unchanged) now also calls `saveJob` after every stage — whether that stage actually ran or was loaded back from a resumed run's artifacts — via a `STAGE_TO_JOB_STAGE` best-fit map from this pipeline's nine stage names onto `jobState.ts`'s `JobStage`. A job is created (`saveJob` with no prior job on disk) before the first stage, identified by the Maps URL (the one thing known before `normalize` produces a business name). A successful run's terminal write records `stage: 'delivery'`, `decision: 'deliver'`, and `finalOutput` (the live URL, or the local `site/index.html` when no deploy target is configured). A failed run's `catch` records the error message onto the job's `errors` array before rethrowing — so a `main.ts` failure is visible on `JobState`, not only in the log file, and never silently leaves the job at `decision: 'running'` while claiming success.
- **No duplicate created:** no second job-state shape, no parallel persistence file — same `lib/workflow/jobState.ts` module `scripts/n8n/stage.ts` uses.
- **Acceptance:** met — see `test/main.jobstate.test.ts` (a resumed real run reaches `delivery`/`deliver` with a `finalOutput` and is reloadable by id; a run that fails partway records the error and never fabricates delivery). Both tests exercise real code — `enhance` really renders, `deploy` really calls `deployToNetlify` with an explicitly-forced-empty token, no mocks.

---

## P1 — needed for the first real product

### P1-1 / T05. Add a Groq adapter — DONE (this pass)
- **What was built:** `lib/ai/providers/groq.ts`, following the exact three-step extension `lib/ai/providers/index.ts` documents (adapter file, `AI_PROVIDER_NAMES`, one line in `ADAPTERS`) and the xai.ts/deepseek.ts template. Registered end to end: `lib/config.ts` (`DEFAULT_MODELS.groq`, `GROQ_API_KEY`/`GROQ_BASE_URL`), `lib/capability/orchestrator.ts` (credential map), `lib/capability/visionInvoker.ts` (explicit not-implemented case, text-only), `lib/capability/models.ts` (catalog entry), `lib/factory/pool.ts` (`DEFAULT_MODELS.groq`, added to `FREE_TIER`), `lib/capability/bindings.ts` (added to `reasoning`, `structured_generation`, `prose_writing`, `creative_direction` — ranked as a second free option right after Gemini, or after the native-schema pair for `structured_generation`).
- **Evidence, not assumption:** fetched live (2026-08-24) — `console.groq.com/docs/overview` (OpenAI-compatible base URL `api.groq.com/openai/v1`), `console.groq.com/docs/rate-limits` (OBSERVED real free tier: GPT-OSS models get 30 req/min, 1,000 req/day, 200,000 tokens/day on the no-cost Developer plan — the actual reason this addresses the single-point-of-failure flag), `console.groq.com/docs/structured-outputs` (OBSERVED native strict `json_schema` support, but only for `openai/gpt-oss-20b` and `openai/gpt-oss-120b` specifically — `defaultModel` is pinned to the 120b id for exactly this reason). Pricing is **not** from a primary source — `groq.com/pricing` is client-rendered and returned no rate table on a live fetch, the same gap this catalogue already documents for Cerebras — so the catalog's cost figures are a third-party-aggregated routing estimate only, correctly marked `priceConfidence: 'estimated'`, per the standing rule against promoting an unverified placeholder to a verified price.
- **Tests:** `test/ai/groq-provider.test.ts` — wiring (registration, catalog, default model, real free allowance); real fetch-stubbed behavioural tests (request shape including strict `json_schema`, response normalization, a 5xx→retryable error, a 4xx→non-retryable error, a timeout); and two real planner tests — Gemini's allowance exhausted fails over to Groq rather than straight to a paid vendor (the literal "failover from Gemini to Groq" acceptance criterion), and Groq's own exhausted allowance is `unpriced-blocked` exactly like Cerebras's until `allowUnverifiedPricingFor` names it. `test/capability/models.test.ts`'s "Cerebras is the one estimated entry" test was updated to include Groq (a real, evidence-grounded change — Groq's pricing has the same unverified-primary-source gap, not a hidden bug).
- **Not done in this pass:** a live call against a real `GROQ_API_KEY` (none available to this agent) — per this repository's own doctrine, an adapter's first real call is its test; that remains a manual/documented step, same status xai.ts/deepseek.ts already carry.

### P1-2. OpenRouter `:free` tier liveness probe — DONE (this pass)
- **Gap:** free-tier membership on OpenRouter is coded but not probed for actual availability before use.
- **Why:** a `:free` model id can silently stop being free or stop existing; without a probe, the anti-lock-in layer can fail invisibly.
- **Resolution:** `scripts/probe-providers.ts` already made one OpenRouter call, but against a single hardcoded (env-overridable) id that was not even the id `models.ts` actually configures this deployment to request — it answered "is the adapter reachable", not "is what the planner will actually pick still alive". Added `scripts/probe-openrouter-free.ts`, which reads every `:free`-suffixed OpenRouter id directly out of `MODEL_CATALOG` (`freeModelIds()` — pure, no network, catalogue-driven so it never drifts from what the planner can select) and probes each one for real (`probeFreeModels()`). A dead id is reported in the script's summary output and its saved `probes/openrouter-free-*.json` record, not silently routed around and not auto-removed from `models.ts` — catalogue edits stay a deliberate follow-up, per this task's own acceptance criteria, not a side effect of running a probe. Absent credentials report `skipped`, matching `probe-providers.ts`'s convention. Added `npm run probe-openrouter-free` alongside the existing `probe-providers` script entry.
- **Files:** new `scripts/probe-openrouter-free.ts`, `test/ai/probe-openrouter-free.test.ts`, `package.json` (new script entry).
- **Tests:** `test/ai/probe-openrouter-free.test.ts` — `freeModelIds()` matches a live re-filter of `MODEL_CATALOG` (proves it isn't a hardcoded copy), every returned id genuinely ends in `:free`, the catalogue currently has at least one such id (so the test can't silently pass on nothing), and two real (unmocked) `probeFreeModels()` calls: a missing credential returns `skipped` with no network call for every id, and an empty id list returns `[]` immediately. Per this task's own acceptance criteria ("the script itself is the artifact; add a lightweight test asserting the script's shape... rather than hitting the live network in CI"), no test makes a live OpenRouter call.
- **Not done in this pass:** an actual live run (`npm run probe-openrouter-free`) against a real `OPENROUTER_API_KEY` — none available to this agent. That remains the manual/documented step, the same status `probe-providers.ts`'s own live run already carries for Groq/xai/deepseek. Today's catalogue has exactly one `:free` id — `google/gemma-4-26b-a4b-it:free` — already carrying a comment recording it was "Verified live, 2026-08-19... confirmed reachable twice via a real call" from a prior manual run; that historical verification is a one-time data point, not a substitute for the repeatable probe this task adds.

### P1-3. Re-verify the Places API credential/scope — BLOCKED on missing credential (code-verified this pass)
- **Gap:** a 401 was reported 2026-08-19 against the live Places API call, not re-verified since.
- **Why:** Evidence Intelligence's live-data path is unconfirmed; every downstream design/content decision depends on evidence being real.
- **Code-level verification done this pass (no gap found):** read `lib/sources/placesApi.ts` in full end to end against T07's checklist — every item is real, not stubbed:
  - **FTID vs. place id:** `isFtid()`/`resolvePlaceId()` correctly distinguish the `0x…:0x…` form every Maps URL actually carries from the `ChIJ…` form the Places API requires, exchanging one for the other via Text Search on name+address, and paying for that exchange only when discovery didn't already hand over a `ChIJ…` id (tested: `isFtid` unit tests, an ftid-exchange integration test, and a same-id-not-paid-for-twice test).
  - **API key handling:** the key travels only in the `X-Goog-Api-Key` header, never in a URL — the photo/media endpoint in particular is fetched with `skipHttpRedirect=true` specifically so the resolved `photoUri` (an ordinary `googleusercontent` URL) replaces the key-bearing media URL before it ever reaches a harvest, `2-raw.json`, or a rendered page (tested: `'never puts the API key in a photo URL'`, asserting the resolved URL does not contain the key).
  - **Field authority / provenance / conflicting data:** `lib/sources/authority.ts`'s `FIELD_AUTHORITY` table and `lib/sources/merge.ts`'s `mergeHarvests()` (earlier-harvest-wins, per field and per item — hours resolve day by day, attributes label by label) are both real, pre-existing, and already covered by `test/sources/merge.test.ts` and `test/sources/provenance.test.ts` — this is not something T07 needed to build, only confirm exists, and it does.
  - **Blocked/missing data:** `harvestPlacesApi` never throws — a missing key, a bad key, a quota wall, a network fault, or a malformed body all fall through to `EMPTY_HARVEST` with a logged reason, so a Places failure degrades a business to "no reviews" rather than fabricating one (tested: skipped-with-no-key, empty-harvest-on-API-refusal, and the file's own doc comment on why this source is never fatal).
  - **Do-not-invent facts:** `parseAccessibility` keeps a stated `false` as a stated absence and states nothing for a field the API didn't answer (tested: `'states nothing about a field the API did not answer'`); `parseReviews` prefers a reviewer's own words over Google's machine translation and requires a checkable `sourceUrl` on every quotation.
- **Still open — genuinely blocked, not skipped:** the live call itself. `PLACES_API_KEY` is not set anywhere in this environment (confirmed: absent from `process.env` and from the project's `.env`) — this agent has no credential to make the call with, so neither outcome T07 asks for (a successful live call, or a live-diagnosed failure with root cause) is achievable right now. The prior session's diagnosis — `401 UNAUTHENTICATED`, described as a **wrong API scope** — points a future credential holder at the likely fix first: confirm the key's Google Cloud project has "Places API (New)" enabled (this integration calls `places.googleapis.com/v1/places`, the New/v1 surface, not the legacy Places API), since a key scoped only to the legacy API would 401 exactly like this against every field mask this file requests.
- **Files:** `lib/sources/placesApi.ts`, `lib/sources/authority.ts`, `lib/sources/merge.ts` (all read, none changed — no gap found to fix).
- **Dependencies:** access to the actual API credential — unmet this pass.
- **Estimated complexity:** trivial (one live call) once a credential is available; small only if that call reveals the scope diagnosis above was incomplete.
- **Acceptance criteria:** one successful live Places API call against a real business, logged, with the credential/scope documented for future reference — **not met this pass, for the single reason above.** Everything checkable without a credential has been checked and is real.

### P1-4. Safety audit of Forge's writing modules (`builder.ts`, `repair.ts`) — DONE (this pass)
- **Gap:** this is the closest point in the codebase to a model writing bytes directly to a customer artifact; it's gated by `anti-ai-gate.ts` but has never received the same static-proof treatment `no-agent-spawn.test.ts` gave the CLI autofix path.
- **Resolution:** `anti-ai-gate.ts` turned out to audit generated *content* (forbidden claims, restraint violations, structural convergence) — it says nothing about where the bytes land, which is what this task actually needed proven. Read every `fs.writeFile`/`fs.copyFile`/`fs.mkdir` call site across all of `lib/forge/*.ts` (`builder.ts`, `repair.ts`, `orchestrator.ts`, `battle.ts`, `browser.ts`, `research.ts`) by hand: every one targets a fixed, code-controlled filename (a string literal, a bounded loop counter, or a real `fs.readdir` directory-entry name) joined onto a caller-supplied base directory — never a path built from the model's generated HTML/CSS/JS, scraped web text, or a business name. Also traced `runId`/`outputDir`/`siteDir` back to their origin at all three automated call sites (`main.ts`, `lib/workflow/runJob.ts`, `scripts/n8n/stage.ts`) and confirmed none of them are ever set from business/profile/model-output data either — only a job id and config-level output directory. Also checked for credential leakage into prompts (`grep` for `apiKey`/`API_KEY`/`process.env` across `lib/forge/`): none — every Forge module reaches a provider only through the `routing.capabilities`/`routing.providers` abstraction, which attaches auth at the HTTP layer, never in prompt text a model could echo back.
- **What shipped:** `test/qa/forge-writing-bounds.test.ts` — a source-scan static proof, same technique as `no-agent-spawn.test.ts`. It enumerates every write/copy call site in `lib/forge/` and checks each against an explicit, hand-verified allowlist of safe path shapes (recursing through local `const` declarations like `htmlPath`/`localFile` to their own `path.join(...)` origin); a second test independently proves the model's own output variables (`generatedHtml`, `code.html/css/js`, `repaired.html/css/js`, `currentHtml/css/js`) never appear as an argument to `path.join`/`path.resolve` anywhere in `builder.ts`/`repair.ts`; a third confirms `auditAntiAIGeneric` is actually imported and called from `orchestrator.ts` (guards against the content-gate proof being vacuous); a fourth guards the scanner itself against a vacuous pass (asserts it actually finds builder.ts's real write/copy call sites). Verified as a real control, not just a passing assertion, by temporarily injecting an unsafe write (`path.join(siteDir, generatedHtml.slice(0,5))`) into a scratch copy of `builder.ts` and confirming both the write-path and generated-content tests fail loudly and by name — then restored the file and re-confirmed green.
- **A real but out-of-scope finding, filed rather than fixed:** see P2-4 below — Forge's generated HTML/CSS/JS is never passed through a content-safety check (no scan for `javascript:` URLs, inline event-handler attributes, or unauthorized external `<script src>` domains) the way the deterministic renderer's `safeHref`/`safeImageUrl`/`escapeText`/`escapeAttribute` (`lib/render/*`) guard hand-templated output. That is a different, larger problem than this task's file-write-containment scope — building a new gate, not proving an existing one — so it is filed, not patched mid-task, per this task's own acceptance criteria.
- **Files:** new `test/qa/forge-writing-bounds.test.ts`. `lib/forge/anti-ai-gate.ts` and the rest of `lib/forge/*.ts` read, none changed — the file-write invariant already held.
- **Tests:** the 4 tests in `test/qa/forge-writing-bounds.test.ts` described above.
- **Acceptance criteria:** met — the static test proves Forge's writing modules cannot write outside a fixed, code-controlled path from any of the enumerated call sites; no fix was needed for that invariant. A related but distinct content-safety gap was found and filed as P2-4, not silently patched.

### P1-5. Confirm CI pipeline exists, add one if not — DONE (this pass, live trigger unverified)
- **Gap:** no `.github/workflows` inspected this pass; unconfirmed whether tests run automatically on push/PR.
- **Why:** cheap, high-value for a project this size and this test-count; prevents regressions from ever reaching `main` unnoticed.
- **Resolution:** confirmed `.github/` did not exist at all (no workflow of any kind, no other CI system — no `.gitlab-ci.yml`/`azure-pipelines.yml`/`.circleci`/`.travis.yml` either, so this is a genuinely new addition, not a duplicate). Added `.github/workflows/test.yml`: `actions/checkout` → `actions/setup-node` (reading the Node version from `package.json`'s own `engines.node` field via `node-version-file`, so the two can't drift apart) → `npm ci` → `npx playwright install-deps chromium` (the repo's `postinstall` already fetches the Chromium binary via `playwright install chromium`; this separately installs the OS-level libraries a fresh Ubuntu runner is missing, without re-downloading the browser) → `npm run typecheck` → `npm test` → `npm run build`. Runs on every push and pull request, with `concurrency`/`cancel-in-progress` so a newer push supersedes an in-flight run rather than racing it.
- **"Must not silently reuse Windows node_modules":** a GitHub-hosted runner is a fresh VM per job — there is no persisted `node_modules` from any prior run, Windows or otherwise, for it to reuse. `npm ci` (not `npm install`) additionally enforces the lockfile itself: it requires `package-lock.json` to exist and match `package.json` exactly, and refuses to run rather than silently rewriting it if they've drifted.
- **Verified locally (all real, this pass, in the cloud sandbox):** `npx tsc -p tsconfig.json --noEmit` and the real `npm run build` both complete cleanly (exit 0, dist/ emitted with no errors) — this had not been run at all earlier in this session, since every other task only ran `typecheck`/`test`, never `build`.
- **Not done this pass — live trigger unverified:** the plan's own acceptance criteria ("push a throwaway branch with a deliberately broken test, confirm the workflow reports failure, then revert") requires pushing to the real `origin` remote (`github.com/remusalex-coder/WebsiteAgent`, confirmed configured) — every commit this session, T01 through T08, has been local-only; this is the first task where the acceptance test itself requires touching the live remote and, by extension, real GitHub Actions minutes on the user's account. Consistent with how this session has treated every other live-external-system verification (Netlify's T04 deploy, Groq's T05 call, OpenRouter's T06 probe, the Places T07 credential), that specific live-trigger check is left as a manual/documented step for whoever has push authorization, not performed by this agent. Everything checkable without pushing — the workflow file's shape, its YAML validity, and that every command it runs succeeds locally — has been checked.
- **Delivery note:** `.github/workflows/*.yml` is a protected path the file-delivery tooling used for every other task this session explicitly refuses to write ("protected file and cannot be written via remote tools") — a deliberate guardrail against an agent silently placing CI/CD workflow files, which can execute with repo credentials on the next push. That guardrail was respected rather than routed around through a different channel. `test.yml` was delivered to the user directly (not committed to the working tree on their machine) — it needs to be added to `.github/workflows/test.yml` and committed by the user, or by an agent explicitly authorized to write that path.
- **Files:** new `.github/workflows/test.yml`.
- **Dependencies:** none.
- **Estimated complexity:** small.
- **Acceptance criteria:** partially met — the workflow exists, is well-formed, and every step it runs has been verified to succeed locally; the live push-and-observe-a-run check is the one remaining manual step, for the reason above.

### P1-6. Extend mechanical registry grounding to one more Director field — DONE (this pass, different subsystem than originally scoped)
- **Gap:** only `runtimePrimitives` has registry-grade enforcement; layout archetype and motion intensity remain advisory-only.
- **Why:** an advisory field can be silently ignored by the renderer with no test catching it — the same gap `runtimePrimitives` had before this session's fix.
- **Architectural finding, resolved rather than left ambiguous:** this task's own file list (`lib/design/directive.ts`, `lib/design/experienceRegistry.ts`) named the classic deterministic-renderer pipeline, but neither a `motionIntensity` nor a `layoutArchetype` field exists on that pipeline's `DesignDirective`/Director type at all — `motionIntensity` lives only in the separate Forge/Experience-Signature pipeline's `ExperienceStrategy` (`lib/forge/types.ts`). The classic pipeline's closest analog, `DirectorPacing` (`'restrained'|'measured'|'cinematic'|'immersive'`), is not advisory in the first place — it already has real mechanical consumers (`densityForPacing`, a `layout.ts` conditional, `conversion.ts`), so there was no matching gap to close there under either literal field name. Meanwhile Forge's `motionIntensity` already had substantial registry-grade enforcement predating this session — `lib/forge/motion.ts`'s closed `MotionIntensity` contract, plus `antiPatternSignals.ts`'s `checkMotionCoherence` (declared durations must match the intensity's contract) and `checkMotionLibraryUsage` (a called library must be actually loaded), both wired into `anti-ai-gate.ts` and gating delivery via `computeForgeVerdict`. Rather than inventing a new, duplicate `motionIntensity` field on the classic Director just to satisfy the plan's literal file list — which the standing "never invent an implementation if an existing one can be reused, never create duplicate... state machines" rule forbids — the actual remaining gap was found by reading `motion.ts`'s own mandatory instruction to the model: include `REDUCED_MOTION_CSS`'s `@media (prefers-reduced-motion: reduce)` block "verbatim, unconditionally". Nothing verified the model actually did. That is exactly this task's shape of gap (a mandatory, advisory-only claim with no mechanical proof), just on the one motion-related claim genuinely still unenforced.
- **What shipped:** `checkReducedMotionSafeguard` in `lib/forge/antiPatternSignals.ts` — mirrors `checkMotionCoherence`'s own scoping exactly: reads the generated CSS for any declared transition/animation duration (reusing the same `parseCssDurationsMs` helper); if any exist and no `@media (prefers-reduced-motion: reduce)` block is present, fails with `REDUCED_MOTION_MISSING`; a build with zero declared durations is exempt regardless of what `motionIntensity` claims (nothing to reduce), the same scoping `checkMotionCoherence` already uses. Wired into `auditAntiAIGeneric` (`anti-ai-gate.ts`) alongside the other motion checks, so it gates delivery the same way.
- **Files:** `lib/forge/antiPatternSignals.ts`, `lib/forge/anti-ai-gate.ts`, `test/forge/antiPatternSignals.test.ts`.
- **Tests:** 5 new tests in `test/forge/antiPatternSignals.test.ts` — animated CSS with no safeguard fails; animated CSS with the safeguard passes; whitespace-insensitive matching; zero-duration CSS is exempt regardless of intensity; the "none"-equivalent (no durations) case is correctly exempt.
- **Acceptance criteria:** met, for the field that actually had a real, currently-unenforced gap — a registry-grade check now exists proving a mandatory motion claim (the reduced-motion safeguard) is mechanically satisfied, not merely instructed, mirroring `checkMotionCoherence`'s own proof shape exactly (`directorRuntimeSeam.test.ts`'s two-stage-seam spirit, adapted to how this subsystem is actually structured — a content-scan gate, not a resolve-then-render seam, because Forge's model output is a whole document, not data flowing through a template).

---

## P2 — quality/scaling

### P2-1. Remove `n8n/factory-v1.json` after confirming no dependents
- **Gap:** legacy `/stage/:name` path, superseded by `businessforge-workflow.json`.
- **Files:** grep for `bf-factory`/`/stage/:name` callers first; delete `n8n/factory-v1.json` and `scripts/n8n/build-factory-workflow.ts` if none found.
- **Estimated complexity:** trivial once confirmed.
- **Acceptance criteria:** grep confirms zero external callers; file removed; n8n README updated to drop the reference.

### P2-2. Add CSS scroll-driven animations / View Transitions API to the runtime primitive registry
- **Gap:** evaluated only; would reduce reliance on vendored Lenis/GSAP for simple cases.
- **Files:** `lib/design/experienceRegistry.ts`, new `lib/runtime/scrollDriven.ts` adapter.
- **Estimated complexity:** medium.
- **Acceptance criteria:** at least one new registry entry with `status:'exists'`, a real adapter, and a test proving it resolves like the existing primitives.

### P2-3. Read `lib/workflow/runner.ts` against `stage.ts`'s hand-written loop and decide wire-in vs. delete — RESOLVED 2026-08-25
- **Gap:** deferred in the Consolidation Map pending this exact comparison.
- **Resolution:** wired in, not deleted. `scripts/n8n/stage.ts`'s `diverge` case now builds its K candidate directions in parallel through `runPool`/`createSerializedWriter`, each direction isolated in a symlinked shadow directory so concurrent builds cannot interleave writes to the shared run root. See `MASTER_INVENTORY.json` A3 and `WORK_QUEUE.json` WQ-004/gap `G-RUNNER-01` for the full writeup; commit `019367f`.
- **Files:** `lib/workflow/runner.ts`, `scripts/n8n/stage.ts` — both changed.
- **Acceptance criteria:** met — wired, with regression tests proving the underlying race was real and is now gone (`test/workflow/candidates.test.ts`), verified natively including `fs.symlink` on Windows.

### P2-4. Content-safety gate for Forge's generated HTML/CSS/JS — queued 2026-08-25 as `WORK_QUEUE.json` `WQ-017`
- **Gap:** found while auditing P1-4/T08 (file-write containment for Forge's writing modules — that task's actual scope, and DONE). Forge's model output is a whole freeform HTML/CSS/JS document, not data slotted into templates, so the deterministic renderer's field-level guards (`safeHref`, `safeImageUrl`, `escapeText`, `escapeAttribute` in `lib/render/*`) don't apply to it and nothing in `lib/forge/` fills the equivalent role. `anti-ai-gate.ts` checks content for genericity/restraint-contract violations, not for `javascript:` URLs, inline event-handler attributes (`onerror=`, etc.), or unauthorized external `<script src>` domains.
- **Why:** lower priority than P1-4's write-path concern (the model can't escape the site directory, per P1-4's proof) but a real gap if a vendor's output ever included such a pattern — accidentally or from a prompt-injection-adjacent path (e.g. scraped web copy quoted back into the HTML-generation prompt in `research.ts`).
- **Files:** likely a new check inside `lib/forge/anti-ai-gate.ts` (alongside `checkAntiPatternSignals`), or a new sibling module.
- **Dependencies:** none.
- **Estimated complexity:** small — a handful of regex/DOM-attribute checks over `code.html`/`code.js`, following the existing `flags.push({code, severity, message})` pattern `auditAntiAIGeneric` already uses.
- **Acceptance criteria:** a new check flags (at minimum) `javascript:`-scheme hrefs, inline `on*=` event-handler attributes, and `<script src>` pointing at a domain outside an explicit allowlist (the motion-library CDNs already named in `motion.ts`); a test proves each one is actually caught, not just declared.

---

## P3 — future

### P3-1. Re-evaluate Dify if a concrete business reason ever appears
No action until a real use case is named — do not integrate speculatively.

### P3-2. Notion mirror
A read-only, generated-from-docs human dashboard, if the user wants one — never an independent source of truth again.

### P3-3. MCP transport completion / skills scaffold
`lib/platform/mcp/*` stdio transport, `lib/platform/skills/*` real implementations — well-structured scaffolding, not load-bearing for the P0 goal. Revisit only after P0/P1 close.
